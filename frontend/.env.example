# The URL of your deployed backend service (e.g. Render, Railway, Fly).
# Do NOT prefix with localhost if running locally, the code defaults to it automatically.
VITE_API_URL=https://cryptidsolver-backend.onrender.com
